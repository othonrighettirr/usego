import { OnModuleInit } from '@nestjs/common';
import { BaileysService } from '../instances/baileys.service';
export declare class ChatwootService implements OnModuleInit {
    private baileys;
    private readonly logger;
    private registeredInstances;
    private contacts;
    private conversations;
    private inboxes;
    private readonly CACHE_TTL;
    constructor(baileys: BaileysService);
    onModuleInit(): Promise<void>;
    private cleanCache;
    registerInstance(instanceId: string): void;
    unregisterInstance(instanceId: string): void;
    private getContactKey;
    private shouldIgnore;
    processMessage(instanceId: string, remoteJid: string, message: string, fromMe: boolean): Promise<void>;
    private getCachedInbox;
    private setCachedInbox;
    private getCachedContact;
    private setCachedContact;
    private getCachedConversation;
    private setCachedConversation;
    private getOrCreateInbox;
    private getOrCreateContact;
    private ensureContactInbox;
    private getOrCreateConversation;
    private sendMessageToChatwoot;
    sendToWhatsApp(instanceId: string, remoteJid: string, message: string): Promise<void>;
    private getHeaders;
    private getBaseUrl;
}
